# Полное обновление бронирований — Test Cases

## Base URL: https://restful-booker.herokuapp.com

## Endpoint
`PUT /booking`

## Тестовые сценарии
- Обновление бронирования с полным набором полей
- Обновление бронирования только с обязательными полями
- Обновление бронирования с отсутствием обязательного поля
- Обновление несуществующего бронирования
- Обновление бронирования с невалидными данными
- Обновление бронирования без авторизации
---

## TC-FULL_UPD-001 — Обновление бронирования с полным набором полей

**Priority:** High  
**Type:** Functional / Positive  
**Method:** `PUT`  
**Endpoint:** `/booking/{id}`

### Предусловия
- Restful Booker API доступен.
- Предварительно создано бронирование.
- Получен действующий токен авторизации.

### Тестовые данные
Не требуются.

### Шаги
1. Отправить GET `/booking/{id}` по предварительно созданному бронированию и сохранить исходные данные.
2. Отправить PUT `/booking/{id}`, поменяв значения каждого поля на другие валидные данные.
3. Проверить HTTP status code.
4. Проверить данные response body.
5. Отправить GET `/booking/{id}` для того же `bookingid`.
6. Сравнить данные из GET с данными, переданными в PUT.

### Ожидаемый результат
- API возвращает `200 OK`.
- Response PUT содержит обновлённые данные.
- GET `/booking/{id}` возвращает 200 OK.
- Данные бронирования после повторного выполнения GET соответствуют данным, переданным в запросе PUT.

### Результат выполнения
**Status:** Passed

**Фактический результат:**
API вернул `200 OK`.
Response PUT содержит переданные при обновлении данные. Последующий GET `/booking/{id}` вернул 200 OK и подтвердил сохранение изменений. Фактические значения соответствуют данным, переданным в PUT.

## TC-FULL_UPD-002 — Обновление бронирования только с обязательными полями

**Priority:** High  
**Type:** Functional / Positive  
**Method:** `PUT`  
**Endpoint:** `/booking/{id}`

### Предусловия
- Restful Booker API доступен.
- Предварительно создано бронирование.
- Получен действующий токен авторизации.

### Тестовые данные
Не требуются.

### Шаги
1. Отправить GET `/booking/{id}` по предварительно созданному бронированию и сохранить исходные данные.
2. Отправить PUT `/booking/{id}`, убрав из запроса `additionalneeds` и поменяв значения других полей валидными данными.
3. Проверить HTTP status code.
4. Проверить данные response body.
5. Отправить GET `/booking/{id}` для того же `bookingid`.
6. Сравнить данные из GET с данными, переданными в PUT.

### Ожидаемый результат
- API возвращает `200 OK`.
- Response PUT содержит обновлённые данные по всем полям, содержит поле `additionalneeds` с первоначальными данными.
- GET `/booking/{id}` возвращает 200 OK.
- Данные бронирования после повторного выполнения GET соответствуют данным, переданным в запросе PUT, поле `additionalneeds` соответствует исходным данным.

### Результат выполнения
**Status:** Passed

**Фактический результат:**
API вернул `200 OK`.
Response PUT содержит переданные при обновлении данные, а также поле `additionalneeds` с первоначальными данными. Последующий GET `/booking/{id}` вернул 200 OK и подтвердил сохранение изменений. Фактические значения соответствуют данным, переданным в PUT. Поле `additionalneeds` не изменено.

## TC-FULL_UPD-003 — Обновление бронирования с отсутствием обязательного поля

**Priority:** High  
**Type:** Functional / Negative  
**Method:** `PUT`  
**Endpoint:** `/booking/{id}`

### Предусловия
- Restful Booker API доступен.
- Предварительно создано бронирование.
- Получен действующий токен авторизации.

### Тестовые данные
Не требуются.

### Шаги
1. Отправить GET `/booking/{id}` по предварительно созданному бронированию и сохранить исходные данные.
2. Отправить PUT `/booking/{id}`, убрав из запроса `firstname`.
3. Проверить HTTP status code.
4. Проверить данные response body.

### Ожидаемый результат
- API возвращает ошибку.
- Обновление без обязательного поля не выполнено

### Результат выполнения
**Status:** Passed

**Фактический результат:**
API вернул `400 Bad Request`.
Запрос на обновление с отсутствием обязательного поля не выполнен.

## TC-FULL_UPD-004 — Обновление несуществующего бронирования

**Priority:** High  
**Type:** Functional / Negative  
**Method:** `PUT`  
**Endpoint:** `/booking/{id}`

### Предусловия
- Restful Booker API доступен.
- Получен действующий токен авторизации.

### Тестовые данные
`/booking/998989`

### Шаги
1. Отправить PUT `/booking/{id}`по несуществующему id.
3. Проверить HTTP status code.
4. Проверить данные response body.

### Ожидаемый результат
- API возвращает ошибку, указывающую на отсутствие id.

### Результат выполнения
**Status:** Failed

**Фактический результат:**
API вернул `405 Method Not Allowed`.
Выполнение запроса GET `/booking/{id}` с указанным `bookingid` отправляет статус `404 Not Found`.

## TC-FULL_UPD-005 — Обновление бронирования с невалидными данными

**Priority:** High  
**Type:** Functional / Negative  
**Method:** `PUT`  
**Endpoint:** `/booking/{id}`

### Предусловия
- Restful Booker API доступен.
- Предварительно создано бронирование.
- Получен действующий токен авторизации.

### Тестовые данные
Не требуются.

### Шаги
1. Отправить GET `/booking/{id}` по предварительно созданному бронированию и сохранить исходные данные.
2. Отправить PUT `/booking/{id}`, указав строковое значение в `totalprice`.
3. Проверить HTTP status code.
4. Проверить данные response body.

### Ожидаемый результат
- API отклоняет запрос с некорректным типом данных для totalprice и возвращает ошибку валидации.

### Результат выполнения
**Status:** Failed

**Фактический результат:**
API возвращает `200 OK`. 
В переданное значение `price` равно `null`. При выполнении GET `/booking/{id}` в теле ответа так же `price` равно `null`

### Дефект
`BUG-001 — API принимает totalprice с некорректным типом данных`

## TC-FULL_UPD-006 — Обновление бронирования без авторизации

**Priority:** High  
**Type:** Functional / Negative  
**Method:** `PUT`  
**Endpoint:** `/booking/{id}`

### Предусловия
- Restful Booker API доступен.
- Предварительно создано бронирование.

### Тестовые данные
Не требуются.

### Шаги
1. Отправить PUT `/booking/{id}` без заполненных данных токена в headers.
3. Проверить HTTP status code.
4. Проверить данные response body.

### Ожидаемый результат
- API возвращает ошибку.
- Обновление без авторизации не выполнено

### Результат выполнения
**Status:** Passed

**Фактический результат:**
API вернул `403 Forbidden`.
Запрос на обновление без авторизации не выполнен.


