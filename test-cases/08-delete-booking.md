# Удаление бронирований — Test Cases

## Base URL: https://restful-booker.herokuapp.com

## Endpoint
`DELETE /booking`

## Тестовые сценарии
- Удаление существующего бронирования
- Удаление несуществующего бронирования
- Удаление бронирования без авторизации
- Повторное удаление одного бронирования
---

## TC-DELETE-001 — Удаление существующего бронирования

**Priority:** High  
**Type:** Functional / Positive  
**Method:** `DELETE`  
**Endpoint:** `/booking/{id}`

### Предусловия
- Restful Booker API доступен.
- Предварительно создано бронирование.
- Получен действующий токен авторизации.

### Тестовые данные
Не требуются.

### Шаги
1. Отправить DELETE `/booking/{id}` по предварительно созданному бронированию.
2. Проверить HTTP status code.
3. Проверить данные response body.
4. Отправить GET `/booking/{id}` для только что удаленного бронирования.
5. Проверить HTTP status code и response body запроса GET.

### Ожидаемый результат
- DELETE возвращает `200 OK`.
- GET `/booking/{id}` по тому же id возвращает `404 Not Found`
- Бронирование удалено.
  
### Результат выполнения
**Status:** Passed

**Фактический результат:**
API вернул `201 Created`.
Последующий GET `/booking/{id}` вернул `404 Not Found` и подтвердил удаление бронирования. 

## TC-DELETE-002 — Удаление несуществующего бронирования

**Priority:** High  
**Type:** Functional / Negative  
**Method:** `DELETE`  
**Endpoint:** `/booking/{id}`

### Предусловия
- Restful Booker API доступен.
- Получен действующий токен авторизации.

### Тестовые данные
`/booking/998989`

### Шаги
1. Отправить DELETE `/booking/{id}`по несуществующему id.
3. Проверить HTTP status code.
4. Проверить данные response body.

### Ожидаемый результат
- API возвращает ошибку, указывающую на отсутствие id.

### Результат выполнения
**Status:** Failed

**Фактический результат:**
API вернул `405 Method Not Allowed`.
Выполнение запроса GET `/booking/{id}` с указанным `bookingid` отправляет статус `404 Not Found`.

## TC-DELETE-003 — Удаление бронирования без авторизации

**Priority:** High  
**Type:** Functional / Negative  
**Method:** `DELETE`  
**Endpoint:** `/booking/{id}`

### Предусловия
- Restful Booker API доступен.
- Предварительно создано бронирование.

### Тестовые данные
Не требуются.

### Шаги
1. Отправить DELETE `/booking/{id}` без заполненных данных токена в headers.
3. Проверить HTTP status code.
4. Проверить данные response body.

### Ожидаемый результат
- API возвращает ошибку.
- Удаление без авторизации не выполнено

### Результат выполнения
**Status:** Passed

**Фактический результат:**
API вернул `403 Forbidden`.
Запрос на удаление без авторизации не выполнен.

## TC-DELETE-004 — Повторное удаление одного бронирования

**Priority:** High  
**Type:** Functional / Negative  
**Method:** `DELETE`  
**Endpoint:** `/booking/{id}`

### Предусловия
- Restful Booker API доступен.
- Предварительно создано бронирование.

### Тестовые данные
Не требуются.

### Шаги
1. Отправить DELETE `/booking/{id}` по предварительно созданному бронированию.
3. Проверить HTTP status code и response body.
5. Повторно отправить DELETE `/booking/{id}` по только что удаленному бронированию.
6. Проверить HTTP status code и response body.

### Ожидаемый результат
- DELETE возвращает `200 OK`.
- Повторный DELETE `/booking/{id}` по тому же id возвращает ошибку`404 Not Found`
- Бронирование удалено.

### Результат выполнения
**Status:** Failed

**Фактический результат:**
Первый запрос API вернул `201 Created`.
При повторном DELETE по этому же бронированию API вернул `405 Method Not Allowed`.
Бронирование удалено.
